# Spruce Workspace Files

This directory contains Spruce project management files.

## Structure

- `artifacts/` - Project artifacts (tasks, features, bugs, etc.)
- `templates/` - Artifact templates
- `views/` - Saved views and filters
- `users/` - User profiles
