---
id: GOL-fv99z9
type: task
title: Delete (Hibiscus) from lag putting table
status: done
---
# Delete (Hibiscus) from lag putting table

Removed `(Hibiscus)` from the `description` field of `lagPuttingTable` in `data/putting.json`. The description now reads: `"Stimp 9 — Square-law calibration. 15" outside trail foot = 20 ft baseline."`
